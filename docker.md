Docker Installation

Open the Docker official website.

Download Docker Desktop.

Install Docker Desktop on your system.

Start the Docker application.

Open Command Prompt / Terminal.

Run the docker run command.

Docker downloads the required image automatically.

Wait until the container starts running.

Open your web browser.

Enter http://localhost:5678 to open the workflow application.
